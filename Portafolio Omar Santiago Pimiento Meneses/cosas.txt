<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="stylesheet" href="css/diseño.css">
<link rel="icon" type="image/x-icon" href="img/icono-1.png">
<title>Portafolio Omar </title>
<style>
  body {
    background-color: #0a0a0a;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    margin: 0;
    padding: 20px;
    gap: 3rem;
    position: relative;
    overflow: hidden;
  }

  .neon-title {
    font-size: 3rem;
    font-weight: 900;
    color: #22d3ee;
    font-family: 'Arial Black', Arial, sans-serif;
    text-align: center;
    text-transform: uppercase;
    letter-spacing: 3px;
    animation: neon-pulse 2s ease-in-out infinite;
    margin: 0;
  }

  .neon-button {
    padding: 1rem 2rem;
    background-color: #000;
    border: 2px solid #22d3ee;
    color: #22d3ee;
    font-size: 1.5rem;
    font-weight: bold;
    border-radius: 12px;
    cursor: pointer;
    font-family: Arial, sans-serif;
    text-transform: uppercase;
    letter-spacing: 2px;
    animation: neon-float 4s ease-in-out infinite;
    transition: all 0.3s ease;
    position: relative;
    z-index: 10;
  }

  .neon-button:hover {
    color: white;
    border-color: #67e8f9;
    box-shadow: 
      0 0 40px rgba(34, 211, 238, 0.8),
      0 0 60px rgba(34, 211, 238, 0.6);
  }

  /* Overlay de transición */
  .transition-overlay {
    position: fixed;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    background: radial-gradient(circle, #22d3ee 0%, #0891b2 50%, #0a0a0a 100%);
    border-radius: 50%;
    transform: translate(-50%, -50%);
    opacity: 0;
    z-index: 9999;
    pointer-events: none;
  }

  .transition-overlay.active {
    animation: explode 0.8s ease-out forwards;
  }

  /* Partículas */
  .particle {
    position: fixed;
    width: 8px;
    height: 8px;
    background: #22d3ee;
    border-radius: 50%;
    pointer-events: none;
    z-index: 9998;
    box-shadow: 0 0 10px #22d3ee, 0 0 20px #22d3ee;
    animation: particle-burst 1s ease-out forwards;
  }

  @keyframes particle-burst {
    to {
      transform: translate(var(--tx), var(--ty));
      opacity: 0;
      scale: 0;
    }
  }

  @keyframes explode {
    0% {
      width: 0;
      height: 0;
      opacity: 1;
    }
    50% {
      opacity: 1;
    }
    100% {
      width: 300vw;
      height: 300vh;
      opacity: 1;
    }
  }

  @keyframes neon-pulse {
    0%, 100% { 
      text-shadow: 
        0 0 10px rgba(34, 211, 238, 0.8), 
        0 0 20px rgba(34, 211, 238, 0.6), 
        0 0 30px rgba(34, 211, 238, 0.4);
    }
    50% { 
      text-shadow: 
        0 0 20px rgba(34, 211, 238, 1), 
        0 0 30px rgba(34, 211, 238, 0.8), 
        0 0 40px rgba(34, 211, 238, 0.6), 
        0 0 50px rgba(34, 211, 238, 0.4);
    }
  }

  @keyframes neon-float {
    0%, 100% { 
      transform: translateY(0px) translateX(0px) scale(1);
      box-shadow: 0 0 20px rgba(34, 211, 238, 0.5);
    }
    25% { 
      transform: translateY(-8px) translateX(4px) scale(1.02);
      box-shadow: 
        0 0 30px rgba(34, 211, 238, 0.6), 
        0 0 50px rgba(34, 211, 238, 0.4);
    }
    50% { 
      transform: translateY(-4px) translateX(-4px) scale(1.03);
      box-shadow: 
        0 0 40px rgba(34, 211, 238, 0.8), 
        0 0 60px rgba(34, 211, 238, 0.6);
    }
    75% { 
      transform: translateY(-8px) translateX(4px) scale(1.02);
      box-shadow: 
        0 0 30px rgba(34, 211, 238, 0.6), 
        0 0 50px rgba(34, 211, 238, 0.4);
    }
  }

  @media (max-width: 768px) {
    .neon-title {
      font-size: 2rem;
      letter-spacing: 2px;
    }
    body {
      gap: 2rem;
    }
  }
</style>
</head>
<body>
  <h1 class="neon-title">Bienvenido a mi portafolio</h1>
  <button class="neon-button" onclick="goToPage('html-5/Comenzar.html')">Comenzar </button>
  
  <!-- Overlay para la transición -->
  <div class="transition-overlay" id="overlay"></div>

  <script>
    function goToPage(url) {
      const button = event.target;
      const rect = button.getBoundingClientRect();
      const centerX = rect.left + rect.width / 2;
      const centerY = rect.top + rect.height / 2;
      
      // Crear 30 partículas que explotan desde el botón
      for (let i = 0; i < 30; i++) {
        createParticle(centerX, centerY);
      }
      
      // Activar overlay de transición
      const overlay = document.getElementById('overlay');
      overlay.classList.add('active');
      
      // Redirigir después de la animación
      setTimeout(() => {
        window.location.href = url;
      }, 800);
    }
    
    function createParticle(x, y) {
      const particle = document.createElement('div');
      particle.className = 'particle';
      particle.style.left = x + 'px';
      particle.style.top = y + 'px';
      
      // Calcular dirección aleatoria
      const angle = Math.random() * Math.PI * 2;
      const velocity = 100 + Math.random() * 200;
      const tx = Math.cos(angle) * velocity;
      const ty = Math.sin(angle) * velocity;
      
      particle.style.setProperty('--tx', tx + 'px');
      particle.style.setProperty('--ty', ty + 'px');
      
      document.body.appendChild(particle);
      
      // Eliminar partícula después de la animación
      setTimeout(() => particle.remove(), 1000);
    }
  </script>
</body>
</html>